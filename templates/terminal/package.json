{
  "private": true,
  "scripts": {
    "dev": "aai",
    "build": "aai deploy --dry-run",
    "deploy": "aai deploy"
  },
  "dependencies": {
    "@jsr/aai__sdk": "*",
    "@jsr/aai__ui": "*",
    "preact": "^10",
    "@preact/signals": "^2",
    "comlink": "^4"
  },
  "devDependencies": {
    "esbuild": "^0.25",
    "preact-render-to-string": "*",
    "typescript": "^5"
  }
}
