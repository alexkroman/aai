/** @module @aai/sdk — re-export barrel for import map compatibility. */

export {
  Agent,
  agentToolsToSchemas,
  DEFAULT_GREETING,
  DEFAULT_INSTRUCTIONS,
  fetchJSON,
  HttpError,
  tool,
} from "@aai/sdk";
export type { AgentOptions, ToolContext, ToolDef, ToolSchema } from "@aai/sdk";
